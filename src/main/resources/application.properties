spring.application.name=bookstore-backend

# local server configurations
server.servlet.contextPath=/api
server.port = 8080

# H2 configurations
spring.datasource.url=jdbc:h2:mem:bookstoredb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=super
spring.jpa.hibernate.ddl-auto=update
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# prevents the trace in errors to be shown to the theUser
server.error.include-stacktrace=never

# Spring Security login credentials, instead of the auto-generated password from the console
#spring.security.theUser.name=user
#spring.security.theUser.password=password